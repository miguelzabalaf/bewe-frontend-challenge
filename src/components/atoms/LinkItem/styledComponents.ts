import styled from "styled-components";
import { colors } from "../../../common/colors";

export const StyledLinkItemWrapper = styled.div`
    border-top: 1px solid ${ colors.border };
    padding: 25px 36px;
`;

export const StyledLinkItemHeader = styled.div`
    display: flex;
    justify-content: space-between;
    align-items: center;
`;

export const StyledLinkItemHeaderUrl = styled.div`

`;

export const StyledLinkItemContent = styled.div`
`;